defaults:
  - model: conformer
  - metrics: example
  - datasets: librispeech_eval_test_clean
  - dataloader: example
  - transforms: asr
  - _self_
text_encoder:
  _target_: src.text_encoder.CTCTextEncoder
inferencer:
  device_tensors: ["spectrogram", "text_encoded"]
  device: auto
  save_path: "librispeech/test-clean"
  seed: 1
  from_pretrained: null
  beam_size: 10
